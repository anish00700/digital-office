# Pinned. claude-agent-sdk bundles a Claude Code binary, so a floating range
# means the agents' behaviour can change under you on a redeploy. Bump on
# purpose: edit here, `./officectl setup`, run the tests, commit.
claude-agent-sdk==0.2.152

# Only needed for OFFICE_BACKEND=api. Harmless to leave installed.
anthropic==1.3.0
